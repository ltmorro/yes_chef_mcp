"""HTML view components for MCP app and FastAPI endpoints.

Built with Vite + React + TypeScript. Run `npm run build` from this
directory to produce the `dist/` output that FastAPI serves.
"""

from pathlib import Path

VIEWS_DIR = Path(__file__).parent
DIST_DIR = VIEWS_DIR / "dist"
